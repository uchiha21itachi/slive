
<!-- <%# if events == [] %>
  <p> "You have no event for the moment"</p>
<%# else %> -->
  <% events.each do |event| %>
  <div class="card">
    <div class="card-description">
      <h3> <%= event.date %> - <%= link_to event.name, event_path(event)%> </h3>
    </div>
  </div>
  <% end %>
